dist: xenial
sudo: false

python:
  - 2.7
  - 3.4
  - 3.5
  - 3.6
  - 3.7

cache:
  - pip

install:
  - pip install tox tox-travis

script:
  - tox

services:
  - redis-server

notifications:
  email:
    recipients:
      - niwi@niwi.be
    on_success: change
    on_failure: change
