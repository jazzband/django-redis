language: python
python:
  - "2.7"
  - "3.2"
install:
  - pip install django redis --use-mirrors
script:
  - python runtests.py
services:
  - redis-server
notifications:
  email:
    recipients:
      - niwi@niwi.be
    on_success: change
    on_failure: change
